// Define a class called Circle with properties radius and methods to calculate area and circumference.
class Scanner  {
    double radius;

    double calculateArea() {
        return Math.PI * radius * radius;
    }

    double calculateCircumference() {
        return 2 * Math.PI * radius;
    }
}
